//? slice() vs substr() vs substring()
slice(index1, index2); // [index1 , (index2-1)]
substr(index, targetWordLength); // params > 0
substring(index1, index2); // if index1 > index2, then [index1, (index2-1)]
//? indexOf() vs search();
indexOf() // position qaytaradi or -1
search() // position qaytaradi or -1, regex bilan ishlata olamiz
